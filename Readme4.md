# Рекурсивный алгоритм Евклида для нахождения НОД

---

## 🧮 Определение

**Наибольший общий делитель (НОД)** двух целых чисел — это наибольшее число, которое делит оба числа без остатка.

**Алгоритм Евклида** — эффективный метод нахождения НОД, основанный на свойстве:
где `a mod b` — остаток от деления `a` на `b`.
Процесс повторяется, пока `b` не станет равным нулю. Тогда НОД равен текущему `a`.

---

## ⚙️ Пошаговая работа алгоритма

| Шаг | Действие                                                                 |
|-----|--------------------------------------------------------------------------|
| 1   | Принимаем на вход два числа `a` и `b`.                                   |
| 2   | Проверяем условие: `b == 0`.                                             |
| 3   | Если `b == 0`, возвращаем `a` — это и есть НОД.                         |
| 4   | Если `b != 0`, вычисляем остаток `r = a % b`.                            |
| 5   | Рекурсивно вызываем `gcd(b, r)`.                                         |
| 6   | Повторяем шаги 2–5, пока `b` не станет `0`.                              |

---

## ⏱ Временная сложность

- **Сложность:** `O(log(min(a, b)))`
- **Причина:** На каждом шаге алгоритм заменяет пару `(a, b)` на `(b, a % b)`, при этом второе число быстро уменьшается — примерно в геометрической прогрессии.
- **Вывод:** Количество рекурсивных вызовов пропорционально логарифму от меньшего из чисел.

---

💡 **Используемые операторы:**
`if`, `%`, `=`, `return`, рекурсивный вызов функции



Ответ на вопрос

# 🧮 Количество перестановок множества из n элементов

## 📘 Определение
Перестановка — это любое возможное упорядоченное расположение всех элементов множества.  
Иными словами, перестановка — это способ расположить элементы в определённом порядке без повторений.

---

## 🔢 Формула

Количество всех перестановок множества из n различных элементов обозначается как:

\[
P_n = n!
\]

где n! (n факториал) вычисляется по формуле:

\[
n! = n × (n - 1) × (n - 2) × \dots × 2 × 1
\]

---
